---
import "../styles/globals.css";
import BaseLayout from "@/layouts/BaseLayout.astro";

/** SEO local para index */
const siteUrl = "https://www.d-essence.com"; // <-- ajusta tu dominio real
const seo = {
  title: "D-Essence Wellness | Modern Aesthetics, Evidence-Based Care",
  description:
    "Skin health, neurotoxins, fillers, microneedling, Aerolase, and more—delivered with medical-grade standards in New Windsor, NY.",
  canonical: "/",
  ogImage: "/og-default.jpg", // coloca tu imagen OG si la tienes
};

/** JSON-LD (HealthAndBeautyBusiness) */
const jsonLd = {
  "@context": "https://schema.org",
  "@type": "HealthAndBeautyBusiness",
  "name": "D-Essence Wellness",
  "image": `${siteUrl}${seo.ogImage}`,
  "url": `${siteUrl}/`,
  "telephone": "+1-716-303-8963",
  "address": {
    "@type": "PostalAddress",
    "streetAddress": "871 Blooming Grove Turnpike Suite 102",
    "addressLocality": "New Windsor",
    "addressRegion": "NY",
    "postalCode": "12553",
    "addressCountry": "US"
  },
  "openingHoursSpecification": [
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Monday", "Tuesday", "Wednesday", "Thursday"],
      "opens": "10:00",
      "closes": "19:00"
    },
    {
      "@type": "OpeningHoursSpecification",
      "dayOfWeek": ["Friday", "Saturday"],
      "opens": "09:00",
      "closes": "18:00"
    }
  ],
  "potentialAction": {
    "@type": "ReserveAction",
    "target": {
      "@type": "EntryPoint",
      "urlTemplate": "https://na02.patientnow.com/a/dessencewellness/OnlineBooking.aspx",
      "inLanguage": "en"
    },
    "result": { "@type": "Reservation", "name": "Consultation Request" }
  }
};
---

<BaseLayout seo={{
  title: seo.title,
  description: seo.description,
  canonical: seo.canonical,
  ogImage: seo.ogImage
}}>
  <Fragment slot="head">
    <script type="application/ld+json" is:inline>{JSON.stringify(jsonLd)}</script>
  </Fragment>

  <!-- ===================== HEADER (CSS-only) ===================== -->
<header class="fixed top-4 left-1/2 -translate-x-1/2 z-[2000] w-full max-w-screen-xl">
  <!-- Desktop -->
  <div class="hidden md:flex items-center justify-center">
    <nav class="relative flex items-center gap-1 rounded-full border border-white/10 bg-black/80 shadow-lg px-3 py-2 leading-none" aria-label="Primary">
      <!-- Wordmark -->
      <a href="/" class="flex items-center px-3 h-10 rounded-full hover:bg-white/5 transition" aria-label="Home">
        <span class="inline-flex items-center h-full">
          <span class="text-white/95 font-semibold tracking-[0.06em] text-[14px] leading-none">D-Essence</span>
          <span class="ml-1.5 text-white/70 font-medium tracking-wide text-[12.5px] leading-none">Wellness</span>
        </span>
      </a>

      <!-- Categorías (desktop) -->
      <ul class="flex items-center gap-1 h-10">
        <!-- Item helper: usa group para abrir panel con hover/focus -->
        <li class="relative group">
          <button class="px-3 h-10 inline-flex items-center rounded-full hover:bg-white/5 transition text-white/90 hover:text-white focus:outline-none">
            Injectables
            <svg class="ml-1 w-4 h-4 opacity-70" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z" clip-rule="evenodd" />
            </svg>
          </button>
          <!-- Panel -->
          <div class="absolute left-1/2 -translate-x-1/2 mt-2 w-[90vw] sm:w-[520px] rounded-2xl border border-white/10 bg-neutral-950 z-[2050] shadow-2xl shadow-black/40 p-1 max-h-[70vh] overflow-auto hidden
                      group-hover:block group-focus-within:block">
            <ul class="divide-y divide-white/10">
              <li><a href="/services/botox" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Botox<span class="opacity-60">→</span></a></li>
              <li><a href="/services/xeomin" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Xeomin<span class="opacity-60">→</span></a></li>
              <li><a href="/services/daxxify" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Daxxify<span class="opacity-60">→</span></a></li>
              <li><a href="/services/botox-for-hyperhidrosis" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Botox for Hyperhidrosis<span class="opacity-60">→</span></a></li>
              <li><a href="/services/prp-microneedling" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">PRP Microneedling<span class="opacity-60">→</span></a></li>
              <li><a href="/services/prf-microneedling" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">PRF Microneedling<span class="opacity-60">→</span></a></li>
              <li><a href="/services/juvederm-fillers" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Dermal Fillers — Juvederm Collection<span class="opacity-60">→</span></a></li>
              <li><a href="/services/smooth-threads" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Threads — Smooth Threads<span class="opacity-60">→</span></a></li>
            </ul>
          </div>
        </li>

        <li class="relative group">
          <button class="px-3 h-10 inline-flex items-center rounded-full hover:bg-white/5 transition text-white/90 hover:text-white focus:outline-none">
            Laser Treatments
            <svg class="ml-1 w-4 h-4 opacity-70" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z" clip-rule="evenodd" />
            </svg>
          </button>
          <div class="absolute left-1/2 -translate-x-1/2 mt-2 w-[90vw] sm:w-[360px] rounded-2xl border border-white/10 bg-neutral-950 z-[2050] shadow-2xl shadow-black/40 p-1 max-h-[70vh] overflow-auto hidden
                      group-hover:block group-focus-within:block">
            <ul class="divide-y divide-white/10">
              <li><a href="/services/aerolase" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Aerolase<span class="opacity-60">→</span></a></li>
              <li><a href="/services/laser-hair-removal" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Laser Hair Removal<span class="opacity-60">→</span></a></li>
            </ul>
          </div>
        </li>

        <li class="relative group">
          <button class="px-3 h-10 inline-flex items-center rounded-full hover:bg-white/5 transition text-white/90 hover:text-white focus:outline-none">
            Aesthetic Services
            <svg class="ml-1 w-4 h-4 opacity-70" viewBox="0 0 20 20" fill="currentColor" aria-hidden="true">
              <path fill-rule="evenodd" d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z" clip-rule="evenodd" />
            </svg>
          </button>
          <div class="absolute left-1/2 -translate-x-1/2 mt-2 w-[90vw] sm:w-[420px] rounded-2xl border border-white/10 bg-neutral-950 z-[2050] shadow-2xl shadow-black/40 p-1 max-h-[70vh] overflow-auto hidden
                      group-hover:block group-focus-within:block">
            <ul class="divide-y divide-white/10">
              <li><a href="/services/waxing-sugaring" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Waxing &amp; Sugaring<span class="opacity-60">→</span></a></li>
              <li><a href="/services/teeth-whitening" class="flex items-center justify-between px-4 py-2.5 hover:bg-white/5 text-white/90">Teeth Whitening<span class="opacity-60">→</span></a></li>
            </ul>
          </div>
        </li>

        <li><a href="/about-us" class="px-3 h-10 inline-flex items-center rounded-full hover:bg-white/5 transition text-white/90 hover:text-white">About Us</a></li>
        <li><a href="/contact-us" class="px-3 h-10 inline-flex items-center rounded-full hover:bg-white/5 transition text-white/90 hover:text-white">Contact Us</a></li>
      </ul>
    </nav>
  </div>

  <!-- Mobile (CSS-only con checkbox peer) -->
  <div class="md:hidden px-4">
    <div class="relative">
      <!-- Checkbox control -->
      <input id="nav-toggle" type="checkbox" class="peer sr-only" aria-hidden="true" />
      <div class="flex items-center justify-between rounded-2xl border border-white/10 bg-neutral-900/75 text-white backdrop-blur-lg shadow-lg px-3 py-2">
        <a href="/" class="py-2" aria-label="Home">
          <span class="inline-flex items-baseline gap-1.5">
            <span class="text-white/95 font-semibold tracking-[0.06em] text-[14px]">D-Essence</span>
            <span class="text-white/70 font-medium tracking-wide text-[12.5px]">Wellness</span>
          </span>
        </a>

        <!-- Label = botón -->
        <label for="nav-toggle" class="p-2 rounded-lg hover:bg-white/10 focus:outline-none focus-visible:ring-2 focus-visible:ring-white/20" aria-controls="mobile-drawer" aria-label="Open menu" role="button" tabindex="0">
          <!-- icon open -->
          <svg class="w-6 h-6 peer-checked:hidden" viewBox="0 0 24 24" fill="none"><path d="M4 7h16M4 12h16M4 17h16" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
          <!-- icon close -->
          <svg class="w-6 h-6 hidden peer-checked:block" viewBox="0 0 24 24" fill="none"><path d="M6 6l12 12M18 6l-12 12" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
        </label>
      </div>

      <!-- Backdrop + Drawer (hermanos del checkbox para usar peer-checked) -->
      <!-- Backdrop -->
      <div class="fixed inset-0 z-[2100] bg-black/60 opacity-0 pointer-events-none transition-opacity duration-200
                  peer-checked:opacity-100 peer-checked:pointer-events-auto"></div>

      <!-- Left blur 10% -->
      <div class="fixed inset-0 right-[8%] z-[2100] bg-black/10 backdrop-blur-[2px] opacity-0 pointer-events-none transition-opacity duration-200
                  peer-checked:opacity-100 peer-checked:pointer-events-auto"></div>

      <!-- Drawer -->
      <aside id="mobile-drawer" class="fixed right-0 top-0 z-[2150] h-full w-[92%] max-w-sm bg-neutral-900 text-white border-l border-white/10 shadow-2xl translate-x-full transition-transform duration-200 will-change-transform
                                     peer-checked:translate-x-0">
        <div class="p-4 h-full flex flex-col">
          <div class="text-xs uppercase tracking-wide text-white/50 px-1">Navigation</div>

          <!-- Acordeones con <details> nativo -->
          <div class="mt-2 space-y-3 overflow-auto">
            <details class="rounded-xl border border-white/10 open:bg-white/5">
              <summary class="list-none cursor-pointer flex items-center justify-between px-4 py-3">
                <span class="font-medium">Injectables</span>
                <svg class="w-5 h-5 transition-transform details-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
              </summary>
              <nav class="border-t border-white/10 py-1">
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/botox">Botox</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/xeomin">Xeomin</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/daxxify">Daxxify</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/botox-for-hyperhidrosis">Botox for Hyperhidrosis</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/prp-microneedling">PRP Microneedling</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/prf-microneedling">PRF Microneedling</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/juvederm-fillers">Dermal Fillers — Juvederm Collection</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/smooth-threads">Threads — Smooth Threads</a>
              </nav>
            </details>

            <details class="rounded-xl border border-white/10 open:bg-white/5">
              <summary class="list-none cursor-pointer flex items-center justify-between px-4 py-3">
                <span class="font-medium">Laser Treatments</span>
                <svg class="w-5 h-5 transition-transform details-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
              </summary>
              <nav class="border-t border-white/10 py-1">
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/aerolase">Aerolase</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/laser-hair-removal">Laser Hair Removal</a>
              </nav>
            </details>

            <details class="rounded-xl border border-white/10 open:bg-white/5">
              <summary class="list-none cursor-pointer flex items-center justify-between px-4 py-3">
                <span class="font-medium">Aesthetic Services</span>
                <svg class="w-5 h-5 transition-transform details-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
              </summary>
              <nav class="border-t border-white/10 py-1">
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/waxing-sugaring">Waxing &amp; Sugaring</a>
                <a class="block px-4 py-2.5 hover:bg-white/5" href="/services/teeth-whitening">Teeth Whitening</a>
              </nav>
            </details>

            <a class="block px-4 py-3 rounded-xl border border-white/10 hover:bg-white/5" href="/about-us">About Us</a>
            <a class="block px-4 py-3 rounded-xl border border-white/10 hover:bg-white/5" href="/contact-us">Contact Us</a>
          </div>

          <!-- Logo centrado abajo -->
          <div class="mt-auto p-4 border-t border-white/10 flex items-center justify-center">
            <a href="/" class="inline-flex items-baseline gap-1.5" aria-label="Home">
              <span class="text-white/95 font-semibold tracking-[0.06em] text-[14px]">D-Essence</span>
              <span class="text-white/70 font-medium tracking-wide text-[12.5px]">Wellness</span>
            </a>
          </div>
        </div>
      </aside>
    </div>
  </div>
</header>


<!-- ======= HEADER SCRIPTS (robustos) ======= -->
<script is:inline>
(function(){
  const init = () => {
    const header   = document.querySelector('header');
    const nav      = document.getElementById('main-nav');
    const items    = nav ? [...nav.querySelectorAll('[data-nav-item]')] : [];
    const panelsOf = () => nav ? [...nav.querySelectorAll('[data-nav-panel]')] : [];

    // ===== Desktop hover =====
    if (nav && items.length) {
      const HOVER_DELAY = 200;
      let t = null;
      const openOnly = (item) => {
        panelsOf().forEach(p => p.classList.add('hidden'));
        items.forEach(i => i.querySelector('[data-nav-trigger]')?.setAttribute('aria-expanded','false'));
        const p = item.querySelector('[data-nav-panel]');
        const b = item.querySelector('[data-nav-trigger]');
        if (p && b) {
          p.classList.remove('hidden');
          b.setAttribute('aria-expanded','true');
          // constrain
          p.style.transform = 'translateX(-50%)';
          const rect = p.getBoundingClientRect();
          const vw = window.innerWidth;
          const PAD = 8;
          let dx = 0;
          if (rect.left < PAD) dx = PAD - rect.left;
          else if (rect.right > vw - PAD) dx = (vw - PAD) - rect.right;
          if (dx !== 0) p.style.transform = `translateX(calc(-50% + ${dx}px))`;
        }
      };
      const closeAll = () => {
        panelsOf().forEach(p => p.classList.add('hidden'));
        items.forEach(i => i.querySelector('[data-nav-trigger]')?.setAttribute('aria-expanded','false'));
      };

      items.forEach(item => {
        const btn = item.querySelector('[data-nav-trigger]');
        const panel = item.querySelector('[data-nav-panel]');
        if (!btn || !panel) return;

        item.addEventListener('mouseenter', () => { if(t) clearTimeout(t); openOnly(item); });
        item.addEventListener('mouseleave', () => { t = setTimeout(closeAll, HOVER_DELAY); });

        btn.addEventListener('focus', () => openOnly(item));
        btn.addEventListener('blur', () => { t = setTimeout(closeAll, HOVER_DELAY); });

        panel.addEventListener('mouseenter', () => { if(t) clearTimeout(t); });
        panel.addEventListener('mouseleave', () => { t = setTimeout(closeAll, HOVER_DELAY); });

        window.addEventListener('resize', () => { if(!panel.classList.contains('hidden')) { /* recalcular en resize */ openOnly(item); } });
      });

      document.addEventListener('click', e => { if (!nav.contains(e.target)) closeAll(); });
      document.addEventListener('keydown', e => { if (e.key === 'Escape') closeAll(); });
    }

    // ===== Mobile drawer =====
    const drawer   = document.getElementById('mobile-drawer');
    const toggle   = document.getElementById('mobile-toggle');
    const panel    = document.getElementById('mobile-panel');
    const backdrop = document.getElementById('mobile-backdrop');
    const iconOpen = document.getElementById('icon-open');
    const iconClose= document.getElementById('icon-close');

    if (drawer && header && drawer.parentElement === header) {
      document.body.appendChild(drawer); // portal
    }

    let locked = false, scrollY = 0, touchHandler = null;
    const lockScroll = () => {
      if (locked) return;
      scrollY = window.scrollY || window.pageYOffset;
      document.body.style.position = 'fixed';
      document.body.style.top = `-${scrollY}px`;
      document.body.style.left = '0';
      document.body.style.right = '0';
      document.body.style.width = '100%';
      document.documentElement.classList.add('overflow-hidden');
      touchHandler = (e)=>{ if(panel && !panel.contains(e.target)) e.preventDefault(); };
      document.addEventListener('touchmove', touchHandler, { passive:false });
      locked = true;
    };
    const unlockScroll = () => {
      if (!locked) return;
      const y = -parseInt(document.body.style.top || '0', 10);
      document.body.style.position = '';
      document.body.style.top = '';
      document.body.style.left = '';
      document.body.style.right = '';
      document.body.style.width = '';
      document.documentElement.classList.remove('overflow-hidden');
      document.removeEventListener('touchmove', touchHandler || (()=>{}));
      window.scrollTo(0, y);
      locked = false;
    };

    const openDrawer = () => {
      if(!drawer || !panel || !backdrop || !toggle) return;
      drawer.classList.remove('hidden');
      requestAnimationFrame(()=>{
        backdrop.classList.remove('opacity-0');
        panel.classList.remove('translate-x-full');
      });
      toggle.setAttribute('aria-expanded','true');
      iconOpen?.classList.add('hidden'); iconClose?.classList.remove('hidden');
      lockScroll();
      panel.querySelector('[data-acc-btn]')?.focus();
    };
    const closeDrawer = () => {
      if(!drawer || !panel || !backdrop || !toggle) return;
      backdrop.classList.add('opacity-0');
      panel.classList.add('translate-x-full');
      toggle.setAttribute('aria-expanded','false');
      iconClose?.classList.add('hidden'); iconOpen?.classList.remove('hidden');
      setTimeout(()=>{ drawer.classList.add('hidden'); unlockScroll(); toggle.focus(); }, 220);
    };

    if (toggle && drawer && panel && backdrop) {
      toggle.addEventListener('click', () => {
        drawer.classList.contains('hidden') ? openDrawer() : closeDrawer();
      });
      // Cerrar tocando fuera del panel
      drawer.addEventListener('click', (e) => { if (!panel.contains(e.target)) closeDrawer(); });
      document.addEventListener('keydown', (e) => { if (e.key === 'Escape' && !drawer.classList.contains('hidden')) closeDrawer(); });
      // Cerrar al navegar en móvil
      drawer.querySelectorAll('a[data-close-drawer]').forEach(a=>{
        a.addEventListener('click', () => { closeDrawer(); });
      });
    }

    // Acordeones móvil
    const accBtns = [...document.querySelectorAll('[data-acc-btn]')];
    accBtns.forEach(btn=>{
      const id = btn.getAttribute('aria-controls');
      const p  = id ? document.getElementById(id) : null;
      const ch = btn.querySelector('svg');
      if (!p) return;
      btn.addEventListener('click', ()=>{
        const expanded = btn.getAttribute('aria-expanded') === 'true';
        accBtns.forEach(b=>{
          const _id = b.getAttribute('aria-controls');
          const _p  = _id ? document.getElementById(_id) : null;
          const _c  = b.querySelector('svg');
          if (_p && b !== btn) { b.setAttribute('aria-expanded','false'); _p.classList.add('hidden'); _c?.classList.remove('rotate-180'); }
        });
        btn.setAttribute('aria-expanded', (!expanded).toString());
        p.classList.toggle('hidden', expanded);
        ch?.classList.toggle('rotate-180', !expanded);
      });
    });
  };

  // Init en escenarios Astro/MPA
  if (document.readyState === 'loading') {
    document.addEventListener('DOMContentLoaded', init, { once: true });
  } else {
    init();
  }
  // Por si usas View Transitions / client router de Astro
  document.addEventListener('astro:page-load', init);
  document.addEventListener('astro:after-swap', init);
})();
</script>


  <!-- ===================== HERO ===================== -->
  <section class="hero-split overflow-x-hidden">
    <div class="container pt-24 pb-16">
      <div class="grid lg:grid-cols-12 gap-10 items-center">
        <!-- Left copy -->
        <div class="lg:col-span-7">
          <div class="text-[12px] inline-flex items-center gap-2 px-3 py-1 border border-white/15 rounded-full mb-4 text-white/80">
            <span>Medical Aesthetics</span><span class="opacity-60">•</span><span>New Windsor, NY</span>
          </div>
          <h1 class="text-white text-4xl sm:text-6xl font-semibold leading-[1.04] tracking-tight">
            Natural refinement, medical rigor.
          </h1>
          <p class="muted mt-4 max-w-xl">
            Subtle, longevity-minded treatments under a clinical standard of care. We prioritize balance, skin health and outcomes that look like you—rested and confident.
          </p>

          <!-- Chips -->
          <div class="mt-5 w-full sm:w-auto">
            <div class="flex flex-nowrap justify-center gap-1 text-[9.5px] leading-tight text-white/80 whitespace-nowrap overflow-x-auto max-w-full px-1
                        sm:overflow-visible sm:flex-wrap sm:justify-start sm:text-[12.5px] sm:gap-2" style="-ms-overflow-style:none;scrollbar-width:none;">
              <span class="px-1 py-0 rounded-full border border-white/15">Nurse-led</span>
              <span class="px-1 py-0 rounded-full border border-white/15">Evidence-based</span>
              <span class="px-1 py-0 rounded-full border border-white/15">FDA-cleared</span>
              <span class="px-1 py-0 rounded-full border border-white/15">Undetectable finish</span>
            </div>
          </div>

          <!-- CTAs -->
          <div class="mt-6 flex flex-wrap justify-center gap-3 sm:justify-start">
            <a href="https://na02.patientnow.com/a/dessencewellness/OnlineBooking.aspx" target="_blank" rel="noopener noreferrer"
              class="btn-neo h-9 text-[13px] leading-none justify-center text-center flex-1 basis-[45%] min-w-[42%] sm:h-11 sm:text-base sm:basis-auto sm:flex-none">
              Book Consultation
            </a>
            <a href="/services"
              class="btn-wire h-9 text-[13px] leading-none justify-center text-center flex-1 basis-[45%] min-w-[42%] sm:h-11 sm:text-base sm:basis-auto sm:flex-none">
              Explore Services
            </a>
          </div>

          <!-- KPIs -->
          <div class="mt-6 text-[10.5px] sm:text-[13px] text-white/80">
            <div class="flex flex-nowrap justify-center items-center gap-2 whitespace-nowrap overflow-x-auto max-w-full px-1
                        sm:overflow-visible sm:flex-wrap sm:justify-start sm:gap-4" style="-ms-overflow-style:none;scrollbar-width:none;">
              <span class="inline-flex items-center gap-1.5">
                <span class="text-white/60">Satisfaction:</span>
                <span aria-label="5 out of 5 stars" role="img" class="text-yellow-400 text-[12px] leading-none">★★★★★</span>
              </span>
              <span class="text-white/25">·</span>
              <span>Conservative approach</span>
              <span class="text-white/25">·</span>
              <span>Skin health first</span>
            </div>
          </div>
        </div>

        <!-- Right: Most booked -->
        <div class="lg:col-span-5">
          <div class="mb-3 sm:mb-4">
            <div class="flex items-center gap-3 text-white/60">
              <div class="h-px bg-white/10 flex-1"></div>
              <div class="text-[11px] sm:text-xs tracking-wider uppercase whitespace-nowrap">Most booked this month</div>
              <div class="h-px bg-white/10 flex-1"></div>
            </div>
          </div>

          <div class="grid grid-cols-2 gap-3">
            <a href="/services/botox" class="group col-span-2 sm:col-span-1 rounded-2xl border border-white/10 p-4 hover:bg-white/5 transition">
              <div class="text-xs uppercase tracking-wide text-white/60">Neurotoxins</div>
              <p class="mt-2 text-white/90 text-sm">Soften frown & forehead lines while preserving expression.</p>
              <div class="mt-3 text-[11px] text-white/50 group-hover:text-white/70">Learn more →</div>
            </a>
            <a href="/services/juvederm-fillers" class="group col-span-2 sm:col-span-1 rounded-2xl border border-white/10 p-4 hover:bg-white/5 transition">
              <div class="text-xs uppercase tracking-wide text-white/60">Juvederm Fillers</div>
              <p class="mt-2 text-white/90 text-sm">Midface structure & contour with restraint for harmony.</p>
              <div class="mt-3 text-[11px] text-white/50 group-hover:text-white/70">Learn more →</div>
            </a>
            <a href="/services/microneedling" class="group col-span-2 rounded-2xl border border-white/10 p-4 hover:bg-white/5 transition">
              <div class="text-xs uppercase tracking-wide text-white/60">Microneedling</div>
              <p class="mt-2 text-white/90 text-sm">Texture, pores, scars—collagen induction with SkinPen®.</p>
              <div class="mt-3 text-[11px] text-white/50 group-hover:text-white/70">Learn more →</div>
            </a>
            <a href="/services/aerolase" class="group col-span-2 rounded-2xl border border-white/10 p-4 hover:bg-white/5 transition">
              <div class="text-xs uppercase tracking-wide text-white/60">Aerolase</div>
              <p class="mt-2 text-white/90 text-sm">Tone, redness & glow using a gentle 650-microsecond laser.</p>
              <div class="mt-3 text-[11px] text-white/50 group-hover:text-white/70">Learn more →</div>
            </a>
          </div>

          <div class="mt-5 grid grid-cols-1 sm:grid-cols-2 gap-3 text-xs">
            <div class="text-white/75">Restore balance. Reveal confidence.</div>
            <div class="sm:text-right">
              <a href="/contact-us" class="underline underline-offset-4 decoration-white/30 hover:decoration-white/70">Talk to a provider</a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ===================== RESULTS ===================== -->
  <section id="results" class="section section-dark">
    <div class="container">
      <div class="grid grid-cols-1 md:grid-cols-12 gap-6 md:gap-10 items-stretch">
        <!-- Left: image -->
        <div class="md:col-span-7 rounded-2xl overflow-hidden ring-1 ring-white/10 bg-white/5">
          <div class="relative aspect-[3/2] md:h-full">
            <picture>
              <source type="image/webp" srcset="/images/home/dermal-fillers-bna-800.webp 800w, /images/home/dermal-fillers-bna-1200.webp 1200w, /images/home/dermal-fillers-bna-1600.webp 1600w" sizes="(min-width: 1024px) 800px, 100vw" />
              <img src="/images/home/dermal-fillers-bna-1200.webp" alt="Dermal fillers — Results" loading="lazy" class="w-full h-full object-cover" />
            </picture>
            <div class="absolute left-4 bottom-4 px-3 py-1.5 rounded-full bg-black/50 backdrop-blur text-xs md:text-sm tracking-wide text-white">
              Dermal fillers — Results
            </div>
          </div>
        </div>

        <!-- Right: copy + CTA -->
        <div class="md:col-span-5 flex">
          <div class="relative rounded-2xl p-8 md:p-10 bg-black/25 ring-1 ring-white/10 w-full flex flex-col justify-between">
            <div>
              <h3 class="text-3xl sm:text-4xl font-semibold tracking-tight">Before &amp; After</h3>
              <div class="mt-5 h-px w-24 bg-white/30"></div>
              <p class="mt-5 text-neutral-300 leading-relaxed">Explore curated outcomes by treatment — organized and easy to compare.</p>
            </div>
            <div class="mt-8 flex justify-center">
              <a href="/gallery" class="btn-outline inline-flex items-center justify-center gap-2">
                BEFORE &amp; AFTER GALLERY
                <svg aria-hidden="true" viewBox="0 0 24 24" class="h-4 w-4">
                  <path d="M5 12h14M13 5l7 7-7 7" fill="none" stroke="currentColor" stroke-width="1.5" stroke-linecap="round" stroke-linejoin="round"/>
                </svg>
              </a>
            </div>
          </div>
        </div>

      </div>
    </div>
  </section>

  <!-- ===================== WHY CHOOSE ===================== -->
  <section id="why-choose" class="section">
    <div class="container text-center">
      <h3 class="text-3xl font-semibold tracking-tight">Why Patients Choose D-Essence</h3>
      <p class="mt-3 text-neutral-400 max-w-2xl mx-auto">Modern aesthetics, clinical precision, and a calm, evidence-based approach focused on you.</p>

      <div class="mt-10 grid grid-cols-1 md:grid-cols-2 gap-6">
        <div class="rounded-2xl bg-white/5 ring-1 ring-white/10 p-6 text-left">
          <div class="flex items-start gap-4">
            <div class="w-12 h-12 rounded-full bg-black/30 ring-1 ring-white/10 flex items-center justify-center shrink-0">
              <svg viewBox="0 0 24 24" class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 3l7 3v5.5c0 4.2-2.9 8-7 9-4.1-1-7-4.8-7-9V6l7-3z"/>
                <path d="M9 12l2 2 4-4"/>
              </svg>
            </div>
            <div>
              <h4 class="text-lg font-semibold">Clinical-grade care</h4>
              <p class="mt-2 text-neutral-400">Medical products, FDA-cleared devices, and protocols grounded in evidence for safe, consistent outcomes.</p>
            </div>
          </div>
        </div>

        <div class="rounded-2xl bg-white/5 ring-1 ring-white/10 p-6 text-left">
          <div class="flex items-start gap-4">
            <div class="w-12 h-12 rounded-full bg-black/30 ring-1 ring-white/10 flex items-center justify-center shrink-0">
              <svg viewBox="0 0 24 24" class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 3l2.2 4.8L19 10l-4.8 2.2L12 19l-2.2-4.8L5 10l4.8-2.2L12 3z"/>
              </svg>
            </div>
            <div>
              <h4 class="text-lg font-semibold">Natural, not overdone</h4>
              <p class="mt-2 text-neutral-400">Subtle, harmonious improvements so you still look like you—only rested and refreshed.</p>
            </div>
          </div>
        </div>

        <div class="rounded-2xl bg-white/5 ring-1 ring-white/10 p-6 text-left">
          <div class="flex items-start gap-4">
            <div class="w-12 h-12 rounded-full bg-black/30 ring-1 ring-white/10 flex items-center justify-center shrink-0">
              <svg viewBox="0 0 24 24" class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M4 7h16M4 17h16"/><circle cx="9" cy="7" r="2"/><circle cx="15" cy="17" r="2"/>
              </svg>
            </div>
            <div>
              <h4 class="text-lg font-semibold">Personalized plans</h4>
              <p class="mt-2 text-neutral-400">We tailor your plan to your goals, timeline, and budget—with clear expectations and follow-up.</p>
            </div>
          </div>
        </div>

        <div class="rounded-2xl bg-white/5 ring-1 ring-white/10 p-6 text-left">
          <div class="flex items-start gap-4">
            <div class="w-12 h-12 rounded-full bg-black/30 ring-1 ring-white/10 flex items-center justify-center shrink-0">
              <svg viewBox="0 0 24 24" class="w-6 h-6" fill="none" stroke="currentColor" stroke-width="1.5">
                <path d="M12 21s-7.5-4.5-9-9c-1.1-3.4 1-6 4-6 2 0 3.2 1.1 5 3 1.8-1.9 3-3 5-3 3 0 5.1 2.6 4 6-1.5 4.5-9 9-9 9z"/>
              </svg>
            </div>
            <div>
              <h4 class="text-lg font-semibold">Comfort & safety</h4>
              <p class="mt-2 text-neutral-400">Gentle techniques, thorough consent, and post-care guidance for a smooth experience end-to-end.</p>
            </div>
          </div>
        </div>
      </div>

      <div class="mt-10 flex justify-center gap-3">
        <a href="/about/meet-the-provider" class="btn-primary">Meet the Provider</a>
        <a href="/about/our-approach" class="btn-outline">Our Approach</a>
      </div>
    </div>
  </section>

  <!-- ===================== REVIEWS ===================== -->
  <section id="testimonials" class="section section-muted">
    <div class="container grid md:grid-cols-2 gap-10 items-start">
      <!-- Left -->
      <div class="space-y-5">
        <h3 class="text-3xl font-semibold tracking-tight">What clients say</h3>
        <p class="text-neutral-500">
          Thoughtful care, natural outcomes, and clear guidance at every step. Our clients appreciate the precision and comfort of their experience.
        </p>
        <div class="flex items-center gap-3">
          <div class="inline-flex items-center gap-2 rounded-full ring-1 ring-neutral-200 px-3 py-1.5 bg-white">
            <svg aria-hidden="true" viewBox="0 0 48 48" class="w-4 h-4">
              <path fill="#FFC107" d="M43.6 20.5H42V20H24v8h11.3C33.8 32.6 29.4 36 24 36c-6.6 0-12-5.4-12-12s5.4-12 12-12c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.7 6.1 29.7 4 24 4 16.1 4 9.3 8.6 6.3 14.7z"/>
              <path fill="#FF3D00" d="M6.3 14.7l6.6 4.9C14.5 16.6 18.9 12 24 12c3.1 0 5.9 1.2 8 3.1l5.7-5.7C34.7 6.1 29.7 4 24 4 16.1 4 9.3 8.6 6.3 14.7z"/>
              <path fill="#4CAF50" d="M24 44c5.3 0 10.2-2 13.8-5.2l-6.4-5.4C29.3 34.5 26.8 36 24 36c-5.3 0-9.7-3.4-11.3-8.1l-6.5 5C9.2 39.6 16 44 24 44z"/>
              <path fill="#1976D2" d="M43.6 20.5H42V20H24v8h11.3c-1.3 3.7-4.7 6-8.3 6-2.3 0-4.4-.8-6-2.1l-6.5 5C17.4 40.6 20.5 42 24 42c10 0 19-7.3 19-20 0-1.3-.1-2.7-.4-3.5z"/>
            </svg>
            <span class="text-xs uppercase tracking-wide text-neutral-900">Google reviews</span>
          </div>
          <a href="https://maps.app.goo.gl/N4y6tDdNHTdkEpJp8" target="_blank" rel="nofollow noopener" class="btn-link text-sm">View on Google</a>
        </div>
        <p class="text-xs text-neutral-400">Reviews shown here are from Google. <a href="https://maps.app.goo.gl/N4y6tDdNHTdkEpJp8" target="_blank" rel="nofollow noopener" class="underline">View on Google</a>.</p>
      </div>

      <!-- Right: Review cards -->
      <div class="grid gap-4">
        <figure class="card p-6 lift ring-1 ring-neutral-200/20">
          <div class="flex items-center gap-2 text-amber-400" aria-hidden="true"><span>★★★★★</span></div>
          <blockquote class="mt-3 text-sm leading-relaxed">“Natural results—I still look like me, just brighter. She explained every step and made me feel at ease.”</blockquote>
          <figcaption class="mt-3 text-xs text-neutral-400">— M.R. • <a href="https://maps.app.goo.gl/N4y6tDdNHTdkEpJp8" target="_blank" rel="nofollow noopener" class="underline">View on Google</a></figcaption>
        </figure>

        <figure class="card p-6 lift ring-1 ring-neutral-200/20">
          <div class="flex items-center gap-2 text-amber-400" aria-hidden="true"><span>★★★★★</span></div>
          <blockquote class="mt-3 text-sm leading-relaxed">“So professional and gentle. I appreciated the clear plan and realistic expectations.”</blockquote>
          <figcaption class="mt-3 text-xs text-neutral-400">— A.L. • <a href="https://maps.app.goo.gl/N4y6tDdNHTdkEpJp8" target="_blank" rel="nofollow noopener" class="underline">View on Google</a></figcaption>
        </figure>

        <figure class="card p-6 lift ring-1 ring-neutral-200/20">
          <div class="flex items-center gap-2 text-amber-400" aria-hidden="true"><span>★★★★★</span></div>
          <blockquote class="mt-3 text-sm leading-relaxed">“Beautiful, subtle outcome and a really calm, caring environment. Highly recommend.”</blockquote>
          <figcaption class="mt-3 text-xs text-neutral-400">— J.S. • <a href="https://maps.app.goo.gl/N4y6tDdNHTdkEpJp8" target="_blank" rel="nofollow noopener" class="underline">View on Google</a></figcaption>
        </figure>
      </div>
    </div>
  </section>

  <!-- ===================== FOOTER ===================== -->
  <footer class="mt-16 border-t border-white/10">
    <div class="container py-10 grid sm:grid-cols-3 gap-8 text-sm text-white/80">
      <div>
        <a href="/" class="inline-flex items-baseline gap-1.5">
          <span class="text-white font-semibold tracking-[0.06em]">D-Essence</span>
          <span class="text-white/70">Wellness</span>
        </a>
        <p class="mt-3 text-white/60">871 Blooming Grove Turnpike Suite 102<br/>New Windsor, NY 12553</p>
        <p class="mt-2 text-white/50">By appointment only</p>
      </div>

      <div class="space-y-2">
        <div class="text-white/60 uppercase tracking-wide text-xs">Navigation</div>
        <a class="block hover:underline" href="/services">Services</a>
        <a class="block hover:underline" href="/about-us">About Us</a>
        <a class="block hover:underline" href="/contact-us">Contact Us</a>
        <a class="block hover:underline" href="/gallery">Before/After</a>
      </div>

      <div class="space-y-2">
        <div class="text-white/60 uppercase tracking-wide text-xs">Book</div>
        <a class="inline-block btn-wire px-4 py-2 rounded-full" target="_blank" rel="noopener noreferrer" href="https://na02.patientnow.com/a/dessencewellness/OnlineBooking.aspx">Online Booking</a>
        <div class="text-white/50 text-xs mt-2">© {new Date().getFullYear()} D-Essence Wellness</div>
      </div>
    </div>
  </footer>
</BaseLayout>
