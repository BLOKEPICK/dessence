---
import BaseLayout from '../../../layouts/BaseLayout.astro';

const seo = {
  title: 'Botox | D-Essence Wellness',
  description:
    'Subtle, natural-looking softening of frown lines, crow’s feet, and forehead lines using onabotulinumtoxinA. Results in ~3–5 days, full effect at 10–14 days, lasting ~3–4 months.',
  canonical: '/services/botox/',
  type: 'website',
  jsonLd: [
    {
      '@context': 'https://schema.org',
      '@type': 'MedicalProcedure',
      name: 'Botulinum toxin type A (Botox) — Cosmetic',
      alternateName: 'Botox',
      procedureType: 'Non-surgical aesthetic treatment',
      bodyLocation: 'Glabella, forehead, periocular (crow’s feet)',
      howPerformed: 'Microinjections into specific facial muscles',
      preparation: 'Avoid blood-thinning supplements/medications when medically appropriate; arrive with clean skin.',
      followup: 'Review at 10–14 days if needed.',
      estimatedCost: 'Varies by area and units',
      availableService: {
        '@type': 'Service',
        serviceType: 'Cosmetic neurotoxin treatment (onabotulinumtoxinA)',
        provider: {
          '@type': 'HealthAndBeautyBusiness',
          name: 'D-Essence Wellness',
          telephone: '+1-716-303-8963',
          address: {
            '@type': 'PostalAddress',
            streetAddress: '871 Blooming Grove Turnpike Suite 102',
            addressLocality: 'New Windsor',
            addressRegion: 'NY',
            postalCode: '12553',
            addressCountry: 'US'
          }
        }
      }
    },
    {
      '@context': 'https://schema.org',
      '@type': 'BreadcrumbList',
      itemListElement: [
        { '@type': 'ListItem', position: 1, name: 'Services', item: 'https://dessencewellness.com/services/' },
        { '@type': 'ListItem', position: 2, name: 'Botox', item: 'https://dessencewellness.com/services/botox/' }
      ]
    }
  ]
};
---

<BaseLayout seo={seo}>
  <!-- ===== HERO (dark) ===== -->
  <section class="bg-neutral-950 text-white">
    <div class="container pt-28 pb-10">
      <div class="max-w-3xl">
        <div class="inline-flex items-center gap-2 text-[11px] uppercase tracking-[0.18em] text-white/70">
          <span>Injectables</span><span class="opacity-40">•</span><span>New Windsor, NY</span>
        </div>
        <h1 class="mt-2 text-4xl sm:text-5xl font-semibold tracking-tight">Botox</h1>
        <p class="mt-4 text-white/80">
          A purified neuromodulator (onabotulinumtoxinA) that relaxes specific muscles to soften dynamic lines—
          keeping expression, not the wrinkles.
        </p>

      
      </div>
    </div>
  </section>

  <!-- ===== CONTENT (white) ===== -->
  <section class="bg-white text-neutral-900">
    <div class="container py-12 sm:py-14">
      <!-- Overview + Side card -->
      <div class="grid lg:grid-cols-12 gap-8 items-start" id="overview">
        <div class="lg:col-span-7">
          <h2 class="text-2xl sm:text-3xl font-semibold tracking-tight">Overview</h2>
          <p class="mt-3 text-neutral-700">
            Botox is a purified form of botulinum toxin type A injected in tiny amounts to relax specific muscles.
            In aesthetics it targets <em>dynamic</em> lines—most commonly the frown lines (glabella), crow’s feet,
            and horizontal forehead lines. Effects usually start around days 3–5, peak at 10–14 days, and last about
            3–4 months (varies by area and metabolism).
          </p>
        </div>

        <aside class="lg:col-span-5">
          <div class="rounded-2xl bg-neutral-50 ring-1 ring-neutral-200 p-5">
            <div class="text-sm font-medium">Good to know</div>
            <ul class="mt-2 grid gap-2 text-sm text-neutral-700">
              {[
                'In-office treatment; ~10–20 minutes for common areas',
                'Minimal downtime; makeup after a few hours if advised',
                'Review/finetune at 10–14 days if appropriate',
                'Results are temporary—customizable over time'
              ].map(item => (
                <li class="inline-flex items-start gap-2">
                  <svg class="w-4 h-4 mt-[2px]" viewBox="0 0 24 24" fill="none"><path d="M20 6L9 17l-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                  <span>{item}</span>
                </li>
              ))}
            </ul>
            <div class="mt-5 flex flex-wrap gap-2">
              <a href="/contact-us/" class="inline-flex items-center rounded-full border border-neutral-300 px-4 py-2 text-sm hover:bg-neutral-50">Ask a question</a>
              <a href="https://na02.patientnow.com/a/dessencewellness/OnlineBooking.aspx" target="_blank" rel="noopener"
                 class="inline-flex items-center rounded-full bg-neutral-900 text-white px-4 py-2 text-sm hover:bg-neutral-800">Book appointment</a>
            </div>
          </div>
        </aside>
      </div>

      <!-- How it works / Candidates / Benefits -->
      <div class="mt-12 grid lg:grid-cols-12 gap-8">
        <section id="how-it-works" class="lg:col-span-7 rounded-2xl bg-white ring-1 ring-neutral-200 p-6">
          <h3 class="text-xl font-semibold">How it works</h3>
          <p class="mt-2 text-neutral-700">
            The neurotoxin temporarily blocks acetylcholine release at the neuromuscular junction, reducing contraction
            of the treated muscle. FDA-approved cosmetic areas include glabellar lines, crow’s feet, and forehead lines.
          </p>
        </section>

        <section id="candidates" class="lg:col-span-5 rounded-2xl bg-white ring-1 ring-neutral-200 p-6">
          <h3 class="text-xl font-semibold">Who it’s for</h3>
          <ul class="mt-2 grid gap-2 text-sm text-neutral-700">
            {[
              'Softening frown, crow’s feet, forehead lines',
              'Preventive care for dynamic lines',
              'Subtle, temporary change with minimal downtime'
            ].map(item => (
              <li class="inline-flex items-start gap-2">
                <svg class="w-4 h-4 mt-[2px]" viewBox="0 0 24 24" fill="none"><path d="M20 6L9 17l-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                <span>{item}</span>
              </li>
            ))}
          </ul>
          <div class="mt-3 text-sm text-neutral-600">
            Not recommended during pregnancy or breastfeeding, with certain neuromuscular disorders, active infection
            at the site, or other clinical contraindications as determined by your provider.
          </div>
        </section>

        <section id="benefits" class="lg:col-span-12 rounded-2xl bg-neutral-50 ring-1 ring-neutral-200 p-6">
          <h3 class="text-xl font-semibold">Benefits</h3>
          <div class="mt-3 grid sm:grid-cols-2 lg:grid-cols-4 gap-4 text-sm text-neutral-700">
            {[
              'Natural-looking softening when dosed anatomically',
              'Quick in-office procedure, minimal downtime',
              'Visible in days; full effect at 1–2 weeks',
              'Temporary (~3–4 months) and adjustable over time'
            ].map(item => (
              <div class="rounded-xl bg-white ring-1 ring-neutral-200 p-4">
                <div class="inline-flex h-8 w-8 items-center justify-center rounded-lg bg-neutral-100 ring-1 ring-neutral-200 mb-2">
                  <svg viewBox="0 0 24 24" class="w-5 h-5" fill="none"><path d="M20 6L9 17l-5-5" stroke="currentColor" stroke-width="1.8" stroke-linecap="round"/></svg>
                </div>
                {item}
              </div>
            ))}
          </div>
        </section>
      </div>

      <!-- What to expect -->
      <section id="expect" class="mt-12 rounded-2xl bg-white ring-1 ring-neutral-200 p-6">
        <h3 class="text-xl font-semibold">What to expect</h3>
        <div class="mt-4 grid md:grid-cols-3 gap-6 text-sm">
          <div>
            <div class="font-medium">Consultation</div>
            <ul class="mt-2 space-y-1 text-neutral-700">
              <li>Dynamic assessment (frown, smile, brow raise)</li>
              <li>Individualized plan by areas & goals</li>
              <li>Skin cleansing, mapping; microinjections</li>
            </ul>
          </div>
          <div>
            <div class="font-medium">Sensation & timing</div>
            <ul class="mt-2 space-y-1 text-neutral-700">
              <li>Brief, mild pinches; most resume routine right away</li>
              <li>Typical session: 10–20 minutes</li>
            </ul>
          </div>
          <div>
            <div class="font-medium">Timeline</div>
            <ul class="mt-2 space-y-1 text-neutral-700">
              <li>Day 0: small redness/pin-points possible</li>
              <li>Days 3–5: early effect</li>
              <li>Days 10–14: full effect / review if needed</li>
              <li>Weeks 10–16: gradual fade</li>
            </ul>
          </div>
        </div>
      </section>

      <!-- Pre-visit / Aftercare -->
      <div class="mt-12 grid lg:grid-cols-2 gap-6">
        <section id="pre-visit" class="rounded-2xl bg-white ring-1 ring-neutral-200 p-6">
          <h3 class="text-xl font-semibold">Pre-visit tips</h3>
          <ul class="mt-3 grid gap-2 text-sm text-neutral-700">
            {[
              'When medically appropriate, avoid NSAIDs/blood thinners, fish oil, ginkgo, and alcohol ~24h (confirm with your doctor if on chronic meds)',
              'Arrive with clean skin (no heavy makeup on treatment areas)',
              'Share medical history, allergies, recent treatments, and expectations'
            ].map(item => (
              <li class="inline-flex items-start gap-2">
                <svg class="w-4 h-4 mt-[2px]" viewBox="0 0 24 24" fill="none"><path d="M20 6L9 17l-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                <span>{item}</span>
              </li>
            ))}
          </ul>
        </section>

        <section id="aftercare" class="rounded-2xl bg-white ring-1 ring-neutral-200 p-6">
          <h3 class="text-xl font-semibold">Aftercare</h3>
          <ul class="mt-3 grid gap-2 text-sm text-neutral-700">
            {[
              'Avoid rubbing/pressure on treated areas for 24h; skip facials/massage that day',
              'Stay upright for a few hours; avoid vigorous exercise for 24h',
              'Mild redness/tenderness/swelling can occur and resolve in 1–2 days'
            ].map(item => (
              <li class="inline-flex items-start gap-2">
                <svg class="w-4 h-4 mt-[2px]" viewBox="0 0 24 24" fill="none"><path d="M20 6L9 17l-5-5" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
                <span>{item}</span>
              </li>
            ))}
          </ul>
          <div class="mt-3 text-xs text-neutral-500">
            Seek medical care urgently for severe symptoms like marked muscle weakness, trouble swallowing/breathing, or vision changes.
          </div>
        </section>
      </div>

      <!-- Safety -->
      <section id="safety" class="mt-12 rounded-2xl bg-neutral-50 ring-1 ring-neutral-200 p-6">
        <h3 class="text-xl font-semibold">Safety & side effects</h3>
        <div class="mt-3 grid md:grid-cols-2 gap-6 text-sm text-neutral-700">
          <div>
            <div class="font-medium">Common & temporary</div>
            <ul class="mt-2 space-y-1">
              <li>Injection-site tenderness, small bruise, headache, mild redness</li>
              <li>Rare: temporary eyelid/eyebrow ptosis, asymmetry, diffusion beyond intended area</li>
            </ul>
          </div>
          <div>
            <div class="font-medium">Important notice</div>
            <ul class="mt-2 space-y-1">
              <li>Always verify licensed providers, medical environment, and approved product</li>
              <li>Reports of counterfeit injectables underscore the need for qualified care</li>
            </ul>
          </div>
        </div>
        <div class="mt-4 text-xs text-neutral-500">
          Educational content only; not medical advice. Your provider will determine candidacy and dosing.
        </div>
      </section>

      <!-- FAQ -->
      <section id="faq" class="mt-12">
        <h2 class="text-xl sm:text-2xl font-semibold tracking-tight">FAQ</h2>
        <div class="mt-6 grid gap-4">
          <details class="group rounded-xl bg-white ring-1 ring-neutral-200 p-5">
            <summary class="list-none cursor-pointer flex items-center justify-between">
              <span class="font-medium text-neutral-900">When will I see results and how long do they last?</span>
              <svg class="w-5 h-5 transition-transform group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
            </summary>
            <p class="mt-3 text-neutral-700 text-sm">
              Early changes around days 3–5, peak at 10–14 days; typical duration ~3–4 months, varying by area and individual.
            </p>
          </details>

          <details class="group rounded-xl bg-white ring-1 ring-neutral-200 p-5">
            <summary class="list-none cursor-pointer flex items-center justify-between">
              <span class="font-medium text-neutral-900">Will I look “frozen”?</span>
              <svg class="w-5 h-5 transition-transform group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
            </summary>
            <p class="mt-3 text-neutral-700 text-sm">
              Our goal is relaxation—not eradication—of movement. Dosing and placement are tailored to maintain natural expression.
            </p>
          </details>

          <details class="group rounded-xl bg-white ring-1 ring-neutral-200 p-5">
            <summary class="list-none cursor-pointer flex items-center justify-between">
              <span class="font-medium text-neutral-900">Is there downtime?</span>
              <svg class="w-5 h-5 transition-transform group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
            </summary>
            <p class="mt-3 text-neutral-700 text-sm">
              Generally no. Avoid rubbing the area and skip vigorous exercise for 24 hours to minimize spread and bruising risk.
            </p>
          </details>

          <details class="group rounded-xl bg-white ring-1 ring-neutral-200 p-5">
            <summary class="list-none cursor-pointer flex items-center justify-between">
              <span class="font-medium text-neutral-900">Can I repeat it safely?</span>
              <svg class="w-5 h-5 transition-transform group-open:rotate-180" viewBox="0 0 20 20" fill="currentColor"><path d="M5.8 7.3a1 1 0 011.4 0L10 10.1l2.8-2.8a1 1 0 111.4 1.4l-3.5 3.5a1 1 0 01-1.4 0L5.8 8.7a1 1 0 010-1.4z"/></svg>
            </summary>
            <p class="mt-3 text-neutral-700 text-sm">
              Yes when clinically indicated and performed by trained providers with approved product. If you stop, muscle activity returns to baseline.
            </p>
          </details>
        </div>
      </section>
    </div>
  </section>

  <!-- Smooth offset scroll for quick-nav -->
  <script is:inline>
    (function () {
      var nav = document.getElementById('quick-nav');
      if (!nav) return;

      function easeInOutQuad(t){ return t<0.5 ? 2*t*t : -1+(4-2*t)*t; }
      function getOffset() {
        var header = document.querySelector('header');
        var base = header ? header.getBoundingClientRect().height : 64;
        return base + 24;
      }
      function smoothScrollTo(targetY, duration) {
        var startY = window.pageYOffset || document.documentElement.scrollTop;
        var startTime = performance.now();
        var D = Math.max(300, Math.min(duration, 900));
        function step(now) {
          var t = Math.min(1, (now - startTime) / D);
          var eased = easeInOutQuad(t);
          window.scrollTo(0, Math.round(startY + (targetY - startY) * eased));
          if (t < 1) requestAnimationFrame(step);
        }
        requestAnimationFrame(step);
      }

      nav.addEventListener('click', function (e) {
        var a = e.target.closest('a[href^="#"]');
        if (!a) return;
        var id = a.getAttribute('href').slice(1);
        var el = document.getElementById(id);
        if (!el) return;

        e.preventDefault();
        var offset = getOffset();
        var rect = el.getBoundingClientRect();
        var targetY = (window.pageYOffset || document.documentElement.scrollTop) + rect.top - offset;
        smoothScrollTo(targetY, 700);

        el.setAttribute('tabindex','-1');
        setTimeout(function(){ el.focus({ preventScroll: true }); el.removeAttribute('tabindex'); }, 750);
      }, { passive: false });
    })();
  </script>
</BaseLayout>
