---
import { redirect } from 'astro:redirect';

// Muy importante:
export const prerender = false;

// GET handler — esto sí funciona dentro de una página
export function GET() {
  return redirect('/', 301);
}
---
