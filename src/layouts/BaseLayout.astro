
<style is:global>
/* --- Service attribute pills: force single line on all viewports --- */
.service-attributes{
  display:flex; align-items:center;
  flex-wrap:nowrap; white-space:nowrap;
  gap:.375rem; /* 6px */
  overflow-x:auto; padding-right:.25rem; /* allow smooth horizontal scroll on small screens */
}
@media (min-width: 768px){
  .service-attributes{ overflow:visible; gap:.75rem; }
}
.service-attributes > li{
  flex:0 0 auto; white-space:nowrap;
  padding:.25rem .5rem; /* 4px 8px */
  font-size: .6875rem; /* 11px */
  line-height: 1.2;
}
@media (min-width: 768px){
  .service-attributes > li{ padding:.375rem .75rem; font-size:.875rem; line-height:1.25; }
}
.service-attributes > li > span{
  width:.25rem; height:.25rem; /* 4px dot on mobile */
}
@media (min-width: 768px){
  .service-attributes > li > span{ width:.375rem; height:.375rem; } /* 6px dot on desktop */
}
</style>

---
import '../styles/globals.css';
import Header from '@/components/Header.astro';
import Footer from '@/components/Footer.astro';
import Seo from '@/components/Seo.astro';
const { seo = {} } = Astro.props;
---
<html lang="en">
  <Seo {...seo} />
  <body>
    <Header />
    <main><slot /></main>
    <Footer />
  </body>
</html>
